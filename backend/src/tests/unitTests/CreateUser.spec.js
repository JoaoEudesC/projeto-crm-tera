describe("Somar dois números", () => {
    it("Deve ser possível somar dois números", () => {
        const number1 = 10;
        const number2 = 10;
        const soma = number1 + number2;

        expect(soma).toBe(20);
    });
});
